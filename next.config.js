/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["ha3zldst15jisuvj.public.blob.vercel-storage.com"],
    },
};

module.exports = nextConfig;
